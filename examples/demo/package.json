{
  "name": "mdmx-demo",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Minimal consumer of the MDMX toolchain: components + content + registry generation",
  "scripts": {
    "generate": "mdmx generate",
    "check": "mdmx check"
  },
  "dependencies": {
    "@mdmx/cli": "workspace:*",
    "@mdmx/core": "workspace:*"
  },
  "devDependencies": {
    "@types/react": "~18.3.18"
  }
}
