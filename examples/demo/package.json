{
  "name": "imdx-demo",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Minimal consumer of the iMDX toolchain: components + content + registry generation",
  "scripts": {
    "generate": "imdx generate",
    "check": "imdx check"
  },
  "dependencies": {
    "@imdx/cli": "workspace:*",
    "@imdx/core": "workspace:*"
  },
  "devDependencies": {
    "@types/react": "~18.3.18"
  }
}
