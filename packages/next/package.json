{
  "name": "@imdx/next",
  "version": "0.3.0",
  "description": "Next.js integration: editor mount, API route handlers, auth, and filesystem content readers",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@imdx/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "~22.10.10"
  }
}
