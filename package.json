{
  "name": "imdx-monorepo",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "pnpm --filter @imdx/core build && pnpm --filter @imdx/provider-github --filter @imdx/cli --filter @imdx/editor --filter @imdx/next build",
    "test": "pnpm --filter @imdx/core build && pnpm -r test",
    "check": "pnpm --filter @imdx/core build && pnpm -r typecheck",
    "verify": "sh scripts/check.sh",
    "generate": "sh scripts/generate.sh",
    "dev:next": "sh scripts/dev-next.sh",
    "dev:playground": "sh scripts/dev-playground.sh",
    "clean": "sh scripts/clean.sh"
  },
  "devDependencies": {
    "typescript": "~5.5.4",
    "vitest": "~2.1.9"
  }
}
